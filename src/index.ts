import { useState } from './useState';
import { createElement } from './createElement';
import { render } from './render';

const Nact = {
    createElement,
    render,
    useState,
};

export default Nact;
